name: RS3 Reference Image Plan

on:
  push:
    branches:
      - rs3-wiki-skill-image-runner
    paths:
      - "tools/rs3_reference/reference_image_plan.py"
      - ".github/workflows/rs3-reference-image-plan.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  plan:
    name: Measure canonical recipe image corpus
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build image plan
        run: python tools/rs3_reference/reference_image_plan.py

      - name: Add summary
        if: always()
        run: |
          if [[ -f rs3_reference_image_plan/SUMMARY.md ]]; then
            cat rs3_reference_image_plan/SUMMARY.md >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload plan
        uses: actions/upload-artifact@v4
        with:
          name: rs3-reference-image-plan
          path: rs3_reference_image_plan/
          if-no-files-found: error
          retention-days: 7
