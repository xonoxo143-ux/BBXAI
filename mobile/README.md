# Mobile

This folder is reserved for a future phone-first interface.

Do not build the mobile app before the audio engine can create a meaningful result.

## Future role

The mobile layer may eventually handle sample capture, sound-role confirmation, arrangement preview, and saving results.

## Rule

The interface should serve the wow moment. It should not become the whole project before the audio idea is proven.
